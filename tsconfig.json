{
  "compilerOptions": {
    "baseUrl": ".", // 解析非相对模块的基地址，默认是当前目录
    "paths": { //路径映射，相对于baseUrl
      "@/*": ["src/*"]
    },
    "types": ["node"]
  },
  "include": ["src/**/*.ts","src/**/**/*.ts","src/*.ts", "src/**/*.tsx", "src/**/*.vue","declarations.d.ts"],
  "exclude": ["node_modules"]
}